-- =============================================================
-- No-op en SQLite: ALTER TABLE ... MODIFY COLUMN con DEFAULT (UUID())
-- no está soportado en SQLite. Hibernate genera los UUIDs en Java
-- antes del INSERT via GenerationType.UUID, por lo que no se
-- necesita un default a nivel de base de datos.
-- =============================================================
